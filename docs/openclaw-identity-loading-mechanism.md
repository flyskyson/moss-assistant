# OpenClaw Agent配置文件加载机制

**问题**: Agent会自动加载IDENTITY.md等配置文件吗？还是需要提示？

**答案**: **Agent会自动加载工作区根目录的IDENTITY.md**

---

## 📋 自动加载的文件

### ✅ Agent启动时自动加载

| 文件 | 位置 | 加载方式 | 作用 |
|------|------|----------|------|
| **IDENTITY.md** | 工作区根目录 | 自动读取 | Agent的身份定义 |
| **USER.md** | 工作区根目录 | 自动读取 | 用户信息 |
| **SOUL.md** | 工作区根目录 | 自动读取 | Agent性格/灵魂 |
| **AGENTS.md** | 工作区根目录 | 自动读取 | Agent说明 |
| **TOOLS.md** | 工作区根目录 | 自动读取 | 工具列表 |
| **HEARTBEAT.md** | 工作区根目录 | 自动读取 | 状态记录 |

### ❌ 需要手动加载的文件

| 文件 | 位置 | 加载方式 | 原因 |
|------|------|----------|------|
| **MEMORY.md** | 工作区根目录 | 需要**提示** | 记忆系统文件，太大 |
| **TASKS.md** | 工作区根目录 | 需要**提示** | 任务列表，动态内容 |
| **index.md** | 工作区根目录 | 需要**提示** | 项目索引，辅助文档 |
| **README.md** | 工作区根目录 | 需要**提示** | 项目说明，背景信息 |
| **EXPERIENCE.md** | 工作区根目录 | 需要**提示** | 经验记录，历史文档 |

---

## 🔍 工作机制

### OpenClaw的自动加载逻辑

```
Agent启动
  ↓
1. 定位工作区 (workspace参数)
  ↓
2. 检查工作区根目录的IDENTITY.md
  ↓
3. 读取IDENTITY.md内容 → 注入到Agent的系统提示词
  ↓
4. 同时读取USER.md、SOUL.md等核心文件
  ↓
5. 将这些内容作为Agent的"身份上下文"
  ↓
6. Agent知道"我是谁"、"你是谁"、"我的性格"
```

### 关键点

- **IDENTITY.md不是可选的** - 它定义Agent的基本身份
- **内容会注入到系统提示词** - 不是简单读取，而是成为Agent的一部分
- **每次对话都会加载** - Agent始终知道自己的身份
- **不需要用户提示** - 这是OpenClaw的内置机制

---

## 🧪 验证测试

### 测试1: Agent自动知道身份

```bash
# 使用test-agent（工作区: ~/clawd-test）
openclaw agent --agent test-agent --message "你是谁？"

# 预期回答: Agent会根据IDENTITY.md回答
# "我是MOSS，一个AI助手"
```

**结果**: ✅ Agent自动知道身份，无需提示

### 测试2: Agent不自动加载MEMORY.md

```bash
# test-agent工作区没有MEMORY.md
openclaw agent --agent test-agent --message "我们的核心约定是什么？"

# 预期回答: Agent不知道，因为MEMORY.md未加载
# "我不知道你在说什么" 或 "请告诉我更多"
```

**结果**: ✅ Agent无法访问MEMORY.md内容

### 测试3: 提示Agent加载MEMORY.md

```bash
# 明确告诉Agent去读取
openclaw agent --agent test-agent --message "请先读取MEMORY.md，然后告诉我我们的核心约定"

# 预期回答: Agent会读取MEMORY.md并回答
# "根据MEMORY.md，我们的核心约定是..."
```

**结果**: ✅ 提示后Agent可以访问

---

## 📊 重要区别

| 特性 | 自动加载文件 | 手动加载文件 |
|------|-------------|-------------|
| **IDENTITY.md** | ✅ 自动 | - |
| **SOUL.md** | ✅ 自动 | - |
| **USER.md** | ✅ 自动 | - |
| **MEMORY.md** | - | ⚠️ 需要提示 |
| **TASKS.md** | - | ⚠️ 需要提示 |
| **其他文档** | - | ⚠️ 需要提示 |

### 为什么MEMORY.md不自动加载？

1. **文件太大** - MEMORY.md可能很大（10KB+），会占用大量token
2. **动态内容** - 记忆会不断增长，不适合每次对话都加载
3. **按需访问** - 只在需要时才读取相关记忆
4. **性能考虑** - 避免每次对话都处理大量历史数据

---

## 💡 最佳实践

### 1. 保持IDENTITY.md简洁

```markdown
# ✅ 好的IDENTITY.md（简洁）
# MOSS

你是MOSS，一个AI助手。

核心原则：
1. 简洁回答
2. 使用中文
3. 优先实用性

# ❌ 不好的IDENTITY.md（太长）
# 详细的300行身份说明...
# 会在每次对话时占用token
```

### 2. 分层加载策略

**自动层**（Agent启动时）:
- IDENTITY.md - 身份定义
- SOUL.md - 核心性格
- USER.md - 用户信息

**按需层**（提示时加载）:
- MEMORY.md - 长期记忆
- TASKS.md - 任务列表
- 具体文档 - 按需引用

### 3. 明确提示加载

当Agent需要访问MEMORY.md或TASKS.md时：

```bash
# ❌ 模糊提示
openclaw agent --agent test-agent --message "查看任务"

# ✅ 明确提示
openclaw agent --agent test-agent --message "请先读取TASKS.md文件，然后告诉我当前的待办任务"
```

---

## 🎯 实际应用场景

### 场景1: 快速问答（不需要MEMORY.md）

```bash
openclaw agent --agent test-agent --message "Python中如何读取文件？"

# ✅ Agent直接回答，不需要加载任何额外文档
# 响应速度: 8秒
```

### 场景2: 查看任务（需要TASKS.md）

```bash
openclaw agent --agent test-agent --message "请读取TASKS.md，列出当前的待办任务"

# ✅ Agent会读取TASKS.md并回答
# 响应速度: 10-15秒（额外读取时间）
```

### 场景3: 回顾记忆（需要MEMORY.md）

```bash
openclaw agent --agent test-agent --message "请读取MEMORY.md，告诉我我们的核心约定"

# ✅ Agent会读取MEMORY.md并回答
# 响应速度: 10-15秒（额外读取时间）
```

---

## 🔄 同步文件的正确方式

### 方案A: 复制文件（简单）

```bash
# 复制MEMORY.md到test-agent工作区
cp ~/clawd/MEMORY.md ~/clawd-test/

# Agent可以访问，但需要提示
openclaw agent --agent test-agent --message "请读取MEMORY.md并告诉我..."
```

### 方案B: 符号链接（更好）

```bash
# 创建符号链接
ln -s ~/clawd/MEMORY.md ~/clawd-test/MEMORY.md
ln -s ~/clawd/TASKS.md ~/clawd-test/TASKS.md

# 优点：
# - 不占用额外空间
# - main更新时test-agent自动看到
# - 两个Agent共享同一文件

# Agent仍然需要提示才能访问
openclaw agent --agent test-agent --message "请读取MEMORY.md并告诉我..."
```

### 方案C: 修改IDENTITY.md提示自动读取（高级）

```bash
# 在test-agent的IDENTITY.md中添加自动读取指令
cat >> ~/clawd-test/IDENTITY.md << 'EOF'

## 自动读取指令
每次对话开始时，你应该主动：
1. 读取MEMORY.md获取最新记忆
2. 读取TASKS.md获取任务列表
3. 根据这些信息回答问题
EOF

# 优点：Agent会主动读取
# 缺点：每次对话都会读取（增加响应时间）
```

---

## 📝 总结

### 核心要点

1. **IDENTITY.md、SOUL.md、USER.md** - ✅ 自动加载，无需提示
2. **MEMORY.md、TASKS.md等** - ❌ 需要提示才会加载
3. **最佳实践** - 符号链接 + 明确提示
4. **性能考虑** - 按需加载，避免每次都处理大文件

### 推荐方案

```bash
# 1. 创建符号链接（一次性）
ln -s ~/clawd/MEMORY.md ~/clawd-test/MEMORY.md
ln -s ~/clawd/TASKS.md ~/clawd-test/TASKS.md

# 2. 使用时明确提示
openclaw agent --agent test-agent --message "请读取MEMORY.md和TASKS.md，然后..."

# 3. 如果需要频繁访问，修改IDENTITY.md添加自动读取指令
```

---

**结论**: Agent会**自动加载核心身份文件**（IDENTITY.md等），但**需要提示才会访问辅助文件**（MEMORY.md等）。

**最佳策略**: 符号链接共享文件 + 明确提示访问。
