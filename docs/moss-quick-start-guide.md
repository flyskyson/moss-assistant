# 🦞 MOSS 快速使用指南

**配置日期**: 2026-02-09
**默认Agent**: test-agent（快速响应）

---

## ✅ 已完成的配置

1. **Shell别名已添加** → `~/.zshrc`
2. **符号链接已创建** → MEMORY.md, TASKS.md自动同步
3. **IDENTITY.md已更新** → 添加读取指令
4. **快速启动脚本** → `~/clawd/scripts/moss-quick-start.sh`

---

## 🚀 立即开始使用

### 方法1: 重新加载配置（推荐）

打开新的终端窗口，或者运行：

```bash
source ~/.zshrc
```

然后就可以使用快捷命令了！

### 方法2: 直接使用完整命令

如果不想重新加载，可以直接使用：

```bash
# 日常使用（test-agent，快速）
openclaw agent --agent test-agent --message "你的问题"

# 查询记忆
openclaw agent --agent test-agent --message "请读取MEMORY.md，告诉我..."

# 查看任务
openclaw agent --agent test-agent --message "请读取TASKS.md，列出当前任务"
```

---

## 📝 可用命令（重新加载后）

```bash
# 快捷别名
moss "你的问题"              # 使用test-agent（快速，11秒）
moss-full "你的问题"          # 使用main agent（慢，137-304秒）
moss-read "问题"             # 查询记忆
moss-tasks                   # 查看待办任务
moss-memory "问题"           # 查询记忆
moss-test                    # 测试连接
```

---

## 💡 使用示例

### 示例1: 快速提问

```bash
moss "Python中如何读取文件？"
# 响应时间: ~11秒
# 无需提示读取文件
```

### 示例2: 查询核心约定

```bash
moss "请读取MEMORY.md，告诉我我们的核心约定是什么"
# 响应时间: ~13秒
# Agent会读取MEMORY.md并回答
```

### 示例3: 查看待办任务

```bash
moss "请读取TASKS.md，列出当前待办任务"
# 响应时间: ~22秒
# Agent会读取TASKS.md并回答
```

---

## ⚠️ 重要提示

### 关于自动读取

**关键发现**: Agent不会真正"自动"读取MEMORY.md和TASKS.md

**你需要明确提示**:
- ❌ "我们的核心约定是什么？" → Agent不知道
- ✅ "请读取MEMORY.md，告诉我核心约定" → Agent会读取

**记忆**: 在问题前加上"请读取MEMORY.md"

### 关于响应时间

| 问题类型 | 响应时间 | 说明 |
|---------|----------|------|
| 普通技术问题 | 11秒 | 无需读取额外文件 |
| 查询记忆 | 13秒 | 需要读取MEMORY.md |
| 查看任务 | 22秒 | 需要读取TASKS.md（内容多） |

---

## 🔄 与main Agent的对比

| 场景 | main | test-agent | 建议 |
|------|------|-----------|------|
| 日常问题 | 137-304秒 | **11秒** | ✅ 使用test-agent |
| 查询记忆 | 137-304秒 | **13秒** | ✅ 使用test-agent |
| 查看任务 | 137-304秒 | **22秒** | ✅ 使用test-agent |
| 复杂分析 | 137-304秒 | **13-22秒** | ✅ 使用test-agent |

**结论**: test-agent在所有场景下都更快

---

## 📊 性能提升

- **平均速度提升**: 10-20倍 ⚡
- **最慢场景**: 22秒（vs 304秒）→ 快14倍
- **最快场景**: 11秒（vs 137秒）→ 快12倍

---

## 🛠️ 故障排除

### 问题: moss命令找不到

**原因**: 别名还没有加载

**解决**:
```bash
# 方法1: 重新加载配置
source ~/.zshrc

# 方法2: 打开新的终端窗口

# 方法3: 使用完整命令
openclaw agent --agent test-agent --message "你的问题"
```

### 问题: Agent说找不到MEMORY.md

**原因**: 符号链接可能损坏

**解决**:
```bash
# 检查符号链接
ls -la ~/clawd-test/MEMORY.md
ls -la ~/clawd-test/TASKS.md

# 如果损坏，重新创建
ln -sf ~/clawd/MEMORY.md ~/clawd-test/MEMORY.md
ln -sf ~/clawd/TASKS.md ~/clawd-test/TASKS.md
```

### 问题: 响应还是很慢

**原因**: 可能实际调用了main agent

**解决**:
```bash
# 确认使用test-agent
openclaw agent --agent test-agent --message "测试"

# 检查配置
openclaw config get agents.defaults.workspace
```

---

## 📚 相关文档

1. [test-agent对比分析](test-agent-vs-main-comparison.md)
2. [Agent加载机制说明](openclaw-identity-loading-mechanism.md)
3. [自动读取配置记录](test-agent-auto-read-configuration.md)
4. [测试结果报告](../reports/test-agent-auto-read-test-results.md)

---

## 🎯 下一步

1. ✅ **重新加载shell配置** - 打开新终端或运行 `source ~/.zshrc`
2. ✅ **测试moss命令** - 运行 `moss "你好"`
3. ✅ **开始使用test-agent** - 享受快速响应！

---

**配置完成时间**: 2026-02-09 15:20
**状态**: ✅ 生产就绪
**建议**: 立即开始使用test-agent作为主要Agent
