# MOSS 记忆系统 - 原理详解

**日期**: 2026-02-07
**目标**: 彻底讲清楚三种不同的记录机制

---

## 🔍 核心问题

你可能混淆了以下概念：
1. **对话记录**（Sessions）- OpenClaw 自动保存
2. **今日 Memory 文件**（`memory/2026-02-07.md`）- 需要手动创建
3. **Diary** - 你提到的概念（需要澄清）

---

## 📊 三种记录机制对比

### 1️⃣ 对话记录（会话文件）

**位置**:
```
~/.openclaw/agents/main/sessions/
├── [session-id].jsonl       # 每个对话会话
└── sessions.json            # 会话索引
```

**格式**: JSONL（每行一个 JSON 对象）

**内容**: 原始对话数据
```json
{"type":"message","id":"xxx","timestamp":"2026-02-07T...","message":{"role":"user","content":"..."}}
{"type":"message","id":"yyy","timestamp":"2026-02-07T...","message":{"role":"assistant","content":"..."}}
{"type":"toolCall","id":"zzz","timestamp":"2026-02-07T...","toolName":"read",...}
```

**谁创建**: ✅ OpenClaw Gateway 自动创建
**谁写入**: ✅ 每条消息自动记录
**谁读取**: ✅ MOSS 可以通过工具读取，但**通常不读**

**用途**:
- 完整的对话历史（包括工具调用）
- 调试和故障排除
- 会话恢复

**问题**:
- ❌ 不是结构化的知识
- ❌ 不便于人类阅读
- ❌ 不能直接作为"记忆"使用

---

### 2️⃣ 今日 Memory 文件

**位置**:
```
~/clawd/memory/
├── 2026-02-05.md            # 周三
├── 2026-02-06.md            # 周四
└── 2026-02-07.md            # 今天（需要创建）
```

**格式**: Markdown（人类可读）

**内容**: 结构化的每日笔记
```markdown
# 📅 2026-02-07 - 今日记录

## 🎯 今日目标
- [x] 调研 MOSS 记忆管理
- [x] 配置 newday 命令

## 📝 对话记录
### 记忆管理调研
**用户问题**: "moss 会自动存吗"
...

## 💡 重要发现
### 1. OpenClaw 的记忆架构
...

## 🔧 技术工作
### 创建的文件
...
```

**谁创建**: ❌ **不是自动的** - 需要你或脚本创建
**谁写入**: 🟡 半自动 - 你告诉 MOSS 写入
**谁读取**: ✅ MOSS 每次会话读取（如果存在）

**用途**:
- 📅 每日计划和目标
- 📝 重要讨论的总结
- 💡 发现和学习
- 🔧 技术工作记录

**newday 命令的作用**:
```bash
newday
```

**效果**:
1. 检查 `~/clawd/memory/2026-02-07.md` 是否存在
2. 如果不存在，创建带模板的文件
3. 如果已存在，提示文件已存在

**原理**:
```bash
#!/bin/bash
TODAY=$(date +%Y-%m-%d)
MEMORY_FILE="~/clawd/memory/${TODAY}.md"

if [ ! -f "$MEMORY_FILE" ]; then
    echo "📝 创建今天的记忆文件: $MEMORY_FILE"
    cat > "$MEMORY_FILE" << EOF
# 📅 ${TODAY} - 今日记录

## 🎯 今日目标
- [ ] 任务 1
- [ ] 任务 2

## 📝 对话记录
[和 MOSS 的对话会记录在这里]

## 💡 重要发现
[今天学到的东西]

## 🔧 技术工作
[技术相关的笔记]

---
*最后更新: $(date +%H:%M)*
EOF
else
    echo "✅ 今天的记忆文件已存在"
fi
```

---

### 3️⃣ 长期记忆（MEMORY.md）

**位置**: `~/clawd/MEMORY.md`

**格式**: Markdown

**内容**: 精炼的长期知识
```markdown
# 🧠 飞天的长期记忆

## 📌 核心身份
- **名字**：MOSS
- **角色**：认知伙伴

## 🔧 技术配置
- **聊天模型**：DeepSeek
- **记忆模型**：nomic-embed-text

## 📅 重要约定
### 2026-02-06 培训计划
...
```

**谁创建**: ✅ 手动创建
**谁写入**: 🟡 半自动 - 每周五自动提取 + 手动补充
**谁读取**: ✅ MOSS 每次会话读取

**用途**:
- 🧠 长期重要知识
- 📌 核心配置信息
- 📅 重要约定和计划
- 🎯 累积的经验教训

---

## 🤔 那么 Diary 是什么？

### 检查结果

我搜索了整个系统，**没有找到 diary 相关的文件或配置**。

```bash
find ~/clawd -name "*diary*" -o -name "*Diary*"
# 结果：无
```

### 可能的情况

#### 可能性 1: 你指的是 Daily Notes

**Daily Notes** = 每日笔记 = `memory/YYYY-MM-DD.md`

**来源**: PARA 系统中的概念
- **Daily Notes**: 每天创建一个笔记，记录当天的工作
- **Weekly Review**: 每周回顾 daily notes，提取重要内容
- **长期记忆**: 精炼的知识存放在 MEMORY.md

#### 可能性 2: 你指的是会话记录（Sessions）

有些人把会话记录叫做 "diary" 或 "journal"，但实际上：
- **会话记录**（Sessions）= JSONL 格式的原始对话
- **不是日记**，而是技术日志

#### 可能性 3: 你想要一个真正的 Diary

如果你想要一个日记系统，可以：
1. 使用 `memory/` 目录（推荐）
2. 或者创建单独的 `diary/` 目录

---

## 📊 三种机制的关系

```
┌─────────────────────────────────────────────────────────┐
│                      你的对话                            │
│                  "你好"、"问题"、"任务"                   │
└──────────────────────┬──────────────────────────────────┘
                       │
         ┌─────────────┴─────────────┐
         │                           │
         ▼                           ▼
┌─────────────────┐         ┌─────────────────┐
│  会话记录（自动） │         │  Memory 文件    │
│                 │         │  （需要创建）    │
│ Sessions/       │         │ memory/YYYY-    │
│ xxx.jsonl       │         │ MM-DD.md        │
│                 │         │                 │
│ • 原始对话      │         │ • 结构化笔记    │
│ • 工具调用      │         │ • 每日目标      │
│ • 时间戳        │         │ • 重要发现      │
│ • JSONL 格式    │         │ • Markdown 格式 │
│                 │         │                 │
│ • 自动保存      │         │ • 需要创建      │
│ • 不常读取      │         │ • 每次读取      │
└─────────────────┘         └─────────┬───────┘
                                     │
                            每周五提取
                                     │
                                     ▼
                          ┌─────────────────┐
                          │  MEMORY.md      │
                          │  （长期记忆）    │
                          │                 │
                          │ • 精炼知识      │
                          │ • 核心配置      │
                          │ • 重要约定      │
                          │                 │
                          │ • 每周五更新    │
                          │ • 每次读取      │
                          └─────────────────┘
```

---

## 💡 为什么需要 Memory 文件？

### 问题 1: 会话记录是原始数据

**会话记录**（Sessions）包含：
- ✅ 所有的对话消息
- ✅ 所有的工具调用
- ✅ 所有的错误和重试

**但**:
- ❌ 不是结构化的知识
- ❌ 不是人类友好的格式
- ❌ 难以查找特定信息
- ❌ 不能直接作为"记忆"使用

**示例**（会话记录）:
```json
{"type":"message","id":"66d34658","parentId":"fcef055a","timestamp":"2026-02-06T23:43:45.327Z","message":{"role":"assistant","content":[{"type":"text","text":"**📋 会话初始化完成**\n\n我已读取以下文件：\n1. ✅ SOUL.md\n2. ✅ USER.md\n..."}],"api":"openai-completions","provider":"openrouter","model":"deepseek/deepseek-chat","usage":{"input":24651,"output":232,"cacheRead":5120,"cacheWrite":0,"totalTokens":30003,"cost":{"input":0.0073953,"output":0.0002784,"cacheRead":0,"cacheWrite":0,"total":0.0076736999999999994}},"stopReason":"stop","timestamp":1770421410618}}
```

**你能看懂吗？** 很难。

---

### 解决方案: Memory 文件（结构化笔记）

**Memory 文件**（`memory/2026-02-07.md`）:
```markdown
## 📝 对话记录

### 记忆管理调研

**用户问题**: "对于记忆，moss会自动存吗，还是要我提醒"

**核心发现**:
- MOSS 记忆系统是"半自动"的
- 对话记录自动保存到会话文件（原始数据）
- Daily Notes 需要手动创建或用脚本触发
- 每周五自动提取到 MEMORY.md
```

**容易阅读** ✅
**容易查找** ✅
**容易理解** ✅

---

## 🚀 完整工作流程

### 步骤 1: 早上（创建 Memory 文件）

```bash
newday
```

**效果**: 创建 `memory/2026-02-07.md`

---

### 步骤 2: 对话中（记录到 Memory 文件）

```
你: "我们讨论了记忆管理，请记录到今天的记忆文件"

MOSS: [读取 memory/2026-02-07.md，追加内容]
```

**效果**: Memory 文件更新

---

### 步骤 3: 周五晚上（自动提取到 MEMORY.md）

**20:10 自动执行**:
1. 读取本周所有 Memory 文件
2. 提取重要内容
3. 更新 MEMORY.md
4. 生成报告

**效果**: 长期记忆更新

---

## 📝 总结对比

| 特性 | 会话记录（Sessions）| Memory 文件 | MEMORY.md |
|------|-------------------|-------------|-----------|
| **位置** | `~/.openclaw/.../sessions/` | `~/clawd/memory/` | `~/clawd/MEMORY.md` |
| **格式** | JSONL | Markdown | Markdown |
| **内容** | 原始对话 | 结构化笔记 | 精炼知识 |
| **创建** | ✅ 自动 | ❌ 手动/脚本 | ❌ 手动 |
| **写入** | ✅ 自动 | 🟡 半自动 | 🟡 半自动 |
| **读取** | ❌ 很少读取 | ✅ 每次读取 | ✅ 每次读取 |
| **用途** | 调试、恢复 | 每日记录 | 长期记忆 |
| **人类可读** | ❌ | ✅ | ✅ |

---

## 🎯 newday 命令的作用

### 作用 1: 创建文件

**问题**: OpenClaw 不会自动创建 `memory/2026-02-07.md`

**解决**: `newday` 命令一键创建

### 作用 2: 提供模板

**问题**: 空文件不知道写什么

**解决**: `newday` 创建带模板的文件

### 作用 3: 提醒记录

**问题**: 容易忘记记录

**解决**: 每天早上运行 `newday`，提醒自己记录

---

## 🔍 如何验证？

### 验证 1: 查看会话记录

```bash
# 查看最新的会话
ls -lt ~/.openclaw/agents/main/sessions/ | head -5

# 查看会话内容（JSONL 格式）
head -20 ~/.openclaw/agents/main/sessions/xxx.jsonl
```

**你会看到**:
- 原始的 JSON 数据
- 所有的消息和工具调用
- 不是人类友好的格式

---

### 验证 2: 查看 Memory 文件

```bash
# 查看今天的 Memory 文件
cat ~/clawd/memory/2026-02-07.md

# 查看 Memory 目录
ls -la ~/clawd/memory/
```

**你会看到**:
- 结构化的 Markdown 文件
- 人类可读的内容
- 今天的计划、讨论、发现

---

### 验证 3: 测试 newday 命令

```bash
# 创建今天的 Memory 文件
newday

# 查看创建的文件
cat ~/clawd/memory/$(date +%Y-%m-%d).md
```

**你会看到**:
- 带模板的 Memory 文件
- 包含 4 个章节（目标、对话、发现、技术）

---

## 💡 核心要点

### 1. 会话记录 ≠ Memory 文件

- **会话记录**: 技术日志（JSONL），自动保存
- **Memory 文件**: 结构化笔记（Markdown），手动创建

### 2. Memory 文件 ≠ Diary

- **Memory 文件**: 工作记录、技术笔记、学习发现
- **Diary**: 个人日记（如果你想创建，可以用 `diary/` 目录）

### 3. newday 的作用

- **创建** Memory 文件（如果不存在）
- **提供** 结构化模板
- **提醒** 你记录重要信息

---

## 🎯 最佳实践

### 每天

1. **早上**: `newday` 创建 Memory 文件
2. **对话中**: 告诉 MOSS 记录重要内容
3. **晚上**: 检查 Memory 文件是否完整

### 每周

1. **周五晚上**: 等待自动提取（20:10）
2. **检查报告**: 查看 `docs/weekly-report-*.md`
3. **补充内容**: 如果有遗漏，手动补充

### 每月

1. **整理 Memory**: 删除过时内容
2. **归档项目**: 完成的项目移到 `archives/`
3. **更新索引**: 更新 `index.md`

---

**现在理解了吗？** 😊

有任何问题随时问我！
