# 🖥️ 创建桌面快捷方式

## Windows 桌面快捷方式

### 方法 1: 手动创建

1. **复制启动脚本**
   - 右键点击 `启动 MOSS.bat`
   - 选择"复制"

2. **粘贴到桌面**
   - 在桌面空白处右键
   - 选择"粘贴快捷方式"

3. **重命名（可选）**
   - 右键点击快捷方式
   - 选择"重命名"
   - 改名为 "MOSS Assistant"

### 方法 2: 创建快捷方式文件

创建文件 `MOSS Assistant.lnk`（使用右键菜单）：

1. 在桌面右键 → 新建 → 快捷方式
2. 浏览到 `C:\Users\flyskyson\moss-assistant\启动 MOSS.bat`
3. 命名为 "MOSS Assistant"
4. 完成

### 方法 3: 自定义图标（高级）

1. 下载图标文件: [moss_icon.ico](./assets/moss_icon.ico)
2. 右键快捷方式 → 属性
3. 点击"更改图标"
4. 选择下载的图标文件
5. 确定

---

## Linux 桌面快捷方式

### 创建 .desktop 文件

1. **创建桌面文件**
   ```bash
   nano ~/.local/share/applications/moss-assistant.desktop
   ```

2. **添加以下内容**
   ```desktop
   [Desktop Entry]
   Version=2.0
   Type=Application
   Name=MOSS Assistant
   Comment=苏格拉底式辩论伙伴 + 全能个人助理
   Exec=/home/你的用户名/moss-assistant/一键启动.sh
   Icon=/home/你的用户名/moss-assistant/assets/moss_icon.png
   Terminal=true
   Categories=Utility;Application;
   ```

3. **添加执行权限**
   ```bash
   chmod +x ~/.local/share/applications/moss-assistant.desktop
   ```

4. **复制到桌面（可选）**
   ```bash
   cp ~/.local/share/applications/moss-assistant.desktop ~/Desktop/
   ```

---

## macOS 桌面快捷方式

### 方法 1: 创建 Automator 应用

1. **打开 Automator**
   - 应用程序 → Automator

2. **创建新文档**
   - 选择"应用程序"

3. **添加"运行 Shell 脚本"**
   - 在搜索框输入"运行 Shell 脚本"
   - 拖到右侧工作区

4. **配置脚本**
   ```bash
   cd /Users/你的用户名/moss-assistant
   ./一键启动.sh
   ```

5. **保存**
   - 文件 → 保存
   - 命名为 "MOSS Assistant"
   - 保存到"应用程序"

6. **添加到 Dock**
   - 从应用程序文件夹拖动 MOSS Assistant 到 Dock

### 方法 2: 创建 .command 文件

1. **创建脚本**
   ```bash
   cat > ~/Desktop/MOSS\ Assistant.command << 'EOF'
   #!/bin/bash
   cd /Users/你的用户名/moss-assistant
   ./一键启动.sh
   EOF
   ```

2. **添加执行权限**
   ```bash
   chmod +x ~/Desktop/MOSS\ Assistant.command
   ```

3. **双击运行**

---

## 🎨 自定义图标

### Windows
- 图标格式: `.ico`
- 推荐尺寸: 256x256
- 放置位置: `assets/moss_icon.ico`

### Linux
- 图标格式: `.png` 或 `.svg`
- 推荐尺寸: 256x256 或 512x512
- 放置位置: `assets/moss_icon.png`

### macOS
- 图标格式: `.icns` 或 `.png`
- 推荐尺寸: 1024x1024
- 放置位置: `assets/moss_icon.icns`

---

## 💡 快捷键建议

创建快捷方式后，可以设置全局快捷键：

### Windows
1. 右键快捷方式 → 属性
2. "快捷键"字段
3. 按下想要的组合键（如 Ctrl+Alt+M）
4. 确定

### Linux
使用系统设置 → 键盘 → 自定义快捷键

### macOS
系统设置 → 键盘 → 键盘快捷键 → 应用快捷键

---

## 🎉 完成后

无论你使用哪种方法，创建桌面快捷方式后：

1. ✅ **一键启动** - 双击桌面图标即可
2. ✅ **固定位置** - 总是在同样的位置
3. ✅ **自定义图标** - 更美观，更易识别
4. ✅ **快捷键** - 键盘一键启动

---

**享受 MOSS Assistant 带来的便捷体验！** 🚀
